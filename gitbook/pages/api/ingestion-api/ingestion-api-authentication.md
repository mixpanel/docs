# Authentication

Most of our Ingestion API calls such as [Import Events](events/import-events.md), [Track Events](events/track-event.md), [User Profiles](user-profiles/profile-set.md), and [Group Profiles](group-profiles/group-set-property.md) only require a [Project Token](../authentication/project-token.md). Because these APIs are often called from our client-side SDKs, we do not want to expose credentials, but we do need to know which project to send data to so we use the Project Token for that purpose. However, a few of the more powerful APIs such as [Lookup Tables](lookup-tables/list-lookup-tables.md) **do require authentication**. For these, it's recommended to use a [Service Account](../authentication/service-accounts.md). We also have legacy support for [Project Secret](../authentication/project-secret.md) but suggest any new integrations use a Service Account.
