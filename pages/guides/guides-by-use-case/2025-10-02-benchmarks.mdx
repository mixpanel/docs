import React from "react";
import { motion } from "framer-motion";

// --- CONFIG: You can edit these arrays to add/remove cards --- //
const sections = [
  {
    id: "benchmarks",
    title: "Benchmarks",
    intro:
      "Explore Mixpanel’s product benchmarks to see how your growth, engagement, and retention metrics compare to peers.",
    cards: [
      {
        title: "Mixpanel Benchmarks",
        href: "https://mixpanel.com/benchmarks/",
        meta: "Report",
        desc:
          "Industry-spanning benchmarks across key lifecycle metrics—growth, engagement, retention, and more.",
      },
    ],
  },
  {
    id: "benchmarks-by-industry",
    title: "Benchmarks by Industry",
    intro:
      "Dive deeper with industry-specific insights. Start with Fintech and Media & Entertainment, and expand as new reports launch.",
    cards: [
      {
        title: "Fintech Benchmarks 2025",
        href: "https://mixpanel.com/m/benchmarks-report-2025-fintech/",
        meta: "Industry Report",
        desc:
          "KPIs and ranges tailored to fintech products—from activation through monetization.",
      },
      {
        title: "Media & Entertainment Benchmarks 2025",
        href: "https://mixpanel.com/m/benchmarks-report-2025-media-entertainment/",
        meta: "Industry Report",
        desc:
          "Streaming and media engagement norms to help teams set realistic targets.",
      },
    ],
  },
  {
    id: "analytics-maturity",
    title: "Analytics Maturity",
    intro:
      "Build a durable analytics practice—start, level up, and operationalize decision-making with Mixpanel.",
    cards: [
      {
        title: "Get Started: Product Analytics Strategy",
        href: "https://mixpanel.com/content/product-analytics-strategy/get-started/",
        meta: "Guide",
        desc:
          "A practical path to define your analytics strategy, align stakeholders, and measure what matters.",
      },
    ],
  },
];

// --- UI Primitives --- //
function Container({ children }: { children: React.ReactNode }) {
  return <div className="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8">{children}</div>;
}

function SectionHeader({ title, intro, id }: { title: string; intro: string; id: string }) {
  return (
    <div className="mb-6 scroll-mt-24" id={id}>
      <h2 className="text-2xl md:text-3xl font-semibold tracking-tight">{title}</h2>
      <p className="mt-2 text-slate-600 leading-relaxed">{intro}</p>
    </div>
  );
}

function Card({ href, title, desc, meta }: { href: string; title: string; desc: string; meta?: string }) {
  return (
    <motion.a
      href={href}
      target="_blank"
      rel="noopener noreferrer"
      initial={{ opacity: 0, y: 8 }}
      whileInView={{ opacity: 1, y: 0 }}
      viewport={{ once: true, margin: "-100px" }}
      transition={{ duration: 0.35 }}
      className="group relative block rounded-2xl border border-slate-200 bg-white p-5 shadow-sm ring-1 ring-transparent transition hover:-translate-y-0.5 hover:shadow-md hover:ring-slate-200 focus:outline-none focus-visible:ring-2 focus-visible:ring-indigo-500"
    >
      <div className="flex items-start gap-4">
        <div className="flex h-10 w-10 shrink-0 items-center justify-center rounded-xl bg-slate-50 ring-1 ring-slate-200">
          <span className="text-lg">📊</span>
        </div>
        <div className="min-w-0">
          <div className="flex items-center gap-2">
            <h3 className="truncate text-base font-semibold text-slate-900 group-hover:underline">
              {title}
            </h3>
            {meta && (
              <span className="inline-flex shrink-0 items-center rounded-md border border-slate-200 bg-slate-50 px-2 py-0.5 text-xs font-medium text-slate-700">
                {meta}
              </span>
            )}
          </div>
          <p className="mt-1 text-sm text-slate-600 line-clamp-2">{desc}</p>
        </div>
      </div>
      <div className="pointer-events-none absolute right-4 top-4 text-slate-400 transition group-hover:text-slate-600">
        ↗
      </div>
    </motion.a>
  );
}

// Switcher lets you preview two visual styles without changing content
function StyleSwitcher({ style, setStyle }: { style: "minimal" | "feature"; setStyle: (s: "minimal" | "feature") => void }) {
  return (
    <div className="inline-flex items-center gap-1 rounded-full border border-slate-200 bg-white p-1 text-sm shadow-sm">
      {(["minimal", "feature"] as const).map((s) => (
        <button
          key={s}
          onClick={() => setStyle(s)}
          className={`rounded-full px-3 py-1 transition ${
            style === s ? "bg-slate-900 text-white" : "text-slate-700 hover:bg-slate-100"
          }`}
        >
          {s === "minimal" ? "Minimal cards" : "Feature tiles"}
        </button>
      ))}
    </div>
  );
}

export default function GuidesBenchmarksMockup() {
  const [style, setStyle] = React.useState<"minimal" | "feature">("minimal");

  return (
    <div className="w-full bg-white text-slate-900">
      {/* Page Header */}
      <div className="border-b border-slate-200 bg-gradient-to-b from-slate-50 to-white">
        <Container>
          <div className="py-10 md:py-14">
            <div className="flex flex-wrap items-center justify-between gap-4">
              <div>
                <h1 className="text-3xl md:text-4xl font-bold tracking-tight">What is Mixpanel – Guides</h1>
                <p className="mt-2 max-w-2xl text-slate-600">
                  A mockup of a Guides landing page with Benchmarks, Industry views, and Analytics Maturity resources.
                </p>
              </div>
              <StyleSwitcher style={style} setStyle={setStyle} />
            </div>
          </div>
        </Container>
      </div>

      {/* Table of Contents */}
      <Container>
        <nav aria-label="Table of contents" className="sticky top-0 z-10 -mx-4 mb-8 bg-white/80 px-4 py-3 backdrop-blur supports-[backdrop-filter]:bg-white/60 sm:rounded-b-2xl">
          <ul className="flex flex-wrap items-center gap-3 text-sm">
            {sections.map((s) => (
              <li key={s.id}>
                <a
                  href={`#${s.id}`}
                  className="inline-flex items-center gap-2 rounded-full border border-slate-200 bg-white px-3 py-1.5 text-slate-700 shadow-sm transition hover:bg-slate-50"
                >
                  <span>#{s.title}</span>
                </a>
              </li>
            ))}
          </ul>
        </nav>
      </Container>

      {/* Sections */}
      <Container>
        <div className="space-y-12 py-6 md:py-10">
          {sections.map((s) => (
            <section key={s.id}>
              <SectionHeader id={s.id} title={s.title} intro={s.intro} />

              {/* Layout switches visually based on style */}
              <div
                className={
                  style === "minimal"
                    ? "grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3"
                    : "grid grid-cols-1 gap-6 sm:grid-cols-2"
                }
              >
                {s.cards.map((c, idx) => (
                  <div key={idx} className={style === "feature" ? "overflow-hidden rounded-3xl border border-slate-200" : ""}>
                    {style === "feature" && (
                      <div className="h-28 w-full bg-slate-100" aria-hidden>
                        {/* Placeholder banner; replace with an image if desired */}
                      </div>
                    )}
                    <div className={style === "feature" ? "p-4 sm:p-5" : ""}>
                      <Card {...c} />
                    </div>
                  </div>
                ))}
              </div>
            </section>
          ))}
        </div>
      </Container>

      {/* Footer note for staging */}
      <div className="border-t border-slate-200">
        <Container>
          <div className="py-8 text-sm text-slate-500">
            <p>
              This page is a UI mockup for staging. Links open in a new tab. Adjust copy, URLs, and card counts in the
              <code className="mx-1 rounded bg-slate-100 px-1.5 py-0.5">sections</code> array.
            </p>
          </div>
        </Container>
      </div>
    </div>
  );
}
