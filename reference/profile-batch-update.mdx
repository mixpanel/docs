---
openapi: "/openapi/ingestion.openapi.yaml POST /engage#profile-batch-update"
title: Update Multiple Profiles
description: "Send a batch of user profile updates in one request; the API returns 200 OK even on validation errors, so check the response body"
---

<Note>
  This API will return a 200 OK even if there are data validation issues. To ensure the request actually succeeded, you need to check the response body.
</Note>
