---
openapi: "/openapi/ingestion.openapi.yaml POST /engage#profile-set"
title: Set User Profile Property
description: "Set or overwrite properties on a user profile; the API returns 200 OK even on validation errors and does not support gzip encoding"
---

<Note>
  This API will return a `200 OK` even if there are data validation issues. To ensure the request actually succeeded, you need to check the response body.
</Note>

<Note>
  This API does not support gzip encoding.
</Note>
